# Context · carb-quiz

**State:** Next.js migration complete. Build passes.

**Done:** App Router, quiz/review/admin, server scoring, images, migrations file.

**Next:** `supabase login && db push`, `vercel login && deploy`, apply schema if tables missing.

**Blockers:** Supabase/Vercel CLI need interactive login on this machine.
